const ImageSearch = ({children}:any) => {
    return(
        <div>
            <h1>
                Find Symbol
            </h1>
            {children}
        </div>
    )
};

export default ImageSearch